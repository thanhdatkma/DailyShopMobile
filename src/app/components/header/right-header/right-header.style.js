import React from "react";
import styled from "styled-components/native";
import { View } from "react-native";

export const RightHeaderIconWrap = styled(View)`
    width: 30px;
    height: 30px;
    position: relative;
`;
